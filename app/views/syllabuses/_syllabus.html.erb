<li>
  <h1><%=syllabus.title%></h1>
  <p></p>In <%= link_to syllabus.topic.name, topic_path(syllabus.topic) %>

  <a data-toggle="collapse" href="#topicForm">(change)</a>

  <p><div id="topicForm" class="collapse">
  <%= form_for syllabus do |f| %>
      <p><%= f.select :topic_id, options_from_collection_for_select(Topic.all, :id, :name)%></p>
      <p><%= f.submit "Save changes", class: "btn btn-primary" %></p>
  <% end %>
</div>   </p>

  <%= link_to '(source)', syllabus.url %>
  <%= raw(syllabus.chnm_cache) %>
</li>